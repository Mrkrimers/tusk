// 3. Вычислите сумму массива целых чисел статичного массива используя рекурсию


const arr = [1, 2, 3.5, 3, 4.2]
let newArr = [];



// function sum(arr_, newArr_) {

//     if (newArr.length == 0) {
//         for (let i = 0; i < arr_.length; i++) {
//             if (Number.isInteger(arr_[i])) {
//                 newArr_.push(arr_[i])
//             }
//         }
//     }

//     if (newArr_.length == 1) { return newArr[0] }

//     return newArr[1] + sum(arr, newArr.slice(1))

// }

// const result = sum(arr, newArr);
// console.log(result);






function sum(arr_) {


    sum()

}



sum(arr)















