// 1. Пользователь вводит имя фамилию. Необходимо создать функцию
// возвращающую строку вида «Привет, {имя} {фамилия}»

const nam = 'sasha';
const surName = 'yarmosh';

function alex(a, b) {
    return `Hi! ${nam} ${surName}`
}

let result = alex(nam, surName);
console.log(result);

