// 18. Дан массив. Вывести каждый отдельный элемент в консоль (for)



let arr = ['я', 'учу', 'javascript', '!'];
alert(arr);
console.log(arr.length);

for (let str = 0; str < arr.length; str++) {
    const element = arr[str];
    console.log(element);
}









